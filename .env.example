# --- Database & Queue ---
DATABASE_URL="postgresql://kutip:kutip_dev_password@127.0.0.1:5434/kutip_dev"
REDIS_URL="redis://localhost:6380"

# --- Clerk (Authentication) ---
# Create an app at https://dashboard.clerk.com, enable Organizations.
# Copy the publishable key and secret key here.
CLERK_SECRET_KEY=""
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"

# --- WhatsApp Webhook (Meta Cloud API) ---
# Choose your own values; Meta must send these when you configure the webhook.
WHATSAPP_WEBHOOK_VERIFY_TOKEN=""
WHATSAPP_WEBHOOK_SECRET=""

# --- Base URL ---
# Your app's public URL, used for payment links and ToyyibPay callbacks.
# Use http://localhost:3000 in development.
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# --- QR-session auth directory (optional) ---
QR_AUTH_DIR="./qr-sessions"